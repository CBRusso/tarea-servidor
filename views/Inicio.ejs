<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inicio - Reservas de Restaurante</title>
    <link rel="stylesheet" href="/public/styles.css"> <!-- Link a archivo CSS -->
</head>
<body>
    <header>
        <h1>Bienvenido, <%= usuario.user %>!</h1>
        <nav>
            <ul>
                <li><a href="/reservas">Mis Reservas</a></li>
                <li><a href="/logout">Cerrar Sesión</a></li>
            </ul>
        </nav>
    </header>

    <main>
        <section>
            <h2>Realizar una nueva reserva</h2>
            <form action="/reservas" method="POST">
                <label for="fecha">Fecha:</label>
                <input type="date" id="fecha" name="fecha" required>

                <label for="hora">Hora:</label>
                <input type="time" id="hora" name="hora" required>

                <label for="personas">Número de personas:</label>
                <input type="number" id="personas" name="personas" min="1" required>

                <button type="submit">Reservar</button>
            </form>
        </section>
<!-- 
        <section>
            <h2>Tus Reservas Anteriores</h2>
            <% if (reservas.length > 0) { %>
                <ul>
                    <% reservas.forEach(function(reserva) { %>
                        <li>
                            <strong>Fecha:</strong> <%= reserva.fecha %> |
                            <strong>Hora:</strong> <%= reserva.hora %> |
                            <strong>Personas:</strong> <%= reserva.personas %>
                        </li>
                    <% }) %>
                </ul>
            <% } else { %>
                <p>No tienes reservas anteriores.</p>
            <% } %> -->
        </section>
    </main>

    <footer>
        <p>&copy; 2024 Restaurante XYZ</p>
    </footer>
</body>
</html>
